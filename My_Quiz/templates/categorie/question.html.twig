{% extends 'base.html.twig' %}

{% block title %}
	{{ categorie.name }}
{% endblock %}

{% block body %}
	<h1 class="text-center">
		{{ categorie.name }}</h1>

	<div class="row">
		<div class="col-8">
			{{ dump(question) }}

			{% for reponse in reponses %}
				<h1>{{ dump(reponse) }}</h1>
			{% endfor %}
		</div>
	</div>

{% endblock %}
